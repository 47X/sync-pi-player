TODO

wczytac do kazdego wideo odpowiadający plik z duration
  for file in videos[]:
    get filename and path
    strip 'mp4'
    add 'duration'
    read file
    duration append

za każdym razem jak się zmienia videoIndex zmieniać durationIndex
